# Phase A audit — 2026-09-24

Project: `qhwmjyzzeggyoktdmqtt` (MomoHouse-Resto).  
Access: Supabase CLI `db query --linked` (user MCP is on a different project; plugin MCP has no permission on this ref).

## Summary

The live database is a **blank Supabase project**. No contract schema has been applied yet. The repo migrations (`table_holds`, `reservation_statuses`, `website_admin_foundation`) exist only as files.

| Check | Result |
| --- | --- |
| `public` tables / columns | **none** |
| `public` / `private` functions | **none** (`private` schema does not exist) |
| RLS policies (`public`, `storage`) | **none** |
| `public` views | **none** |
| App enums (`house_id`, `reservation_status`, …) | **none** (only Auth/storage built-ins) |
| Overlap query | **not run on rows** — `reservations` does not exist. Treat as **0 overlaps**. |
| `start_time` / `hold_minutes` types | **N/A** (table missing). No silent cast required. |
| `btree_gist` | **not enabled** |
| `pg_cron` | **not enabled** |
| `pgcrypto` | enabled (1.3) |
| `supabase_migrations.schema_migrations` | **does not exist** |

Expected-from-repo conflicts (named checks, nullable `contact_messages.house`) **do not apply** until the older migrations run. Applying the reference SQL on this empty project is the clean path.

## Extensions present

`pg_stat_statements` 1.11, `pgcrypto` 1.3, `plpgsql` 1.0, `supabase_vault` 0.3.1, `uuid-ossp` 1.1.

## Functions / policies / views

Empty. No leftover `private.is_privileged`, `windows_overlap`, or insecure admin views.

## Gate

- Overlaps: none to release.
- Backup: empty project; dump taken before apply (or skipped as there is no app data).
- `pg_cron`: must be enabled (Dashboard or `create extension`) before section 23 of the reference SQL schedules jobs. Migration still succeeds without it and raises a notice.

Human dashboard still needed later: disable sign-ups, min password 12, redirect URLs, Resend SMTP.
